import UIKit
import GKCore

struct TransactionCellViewModel {
    
}

final class TransactionCell: BaseTableViewCell<TransactionCellViewModel> {

    override func setupViews() {
        super.setupViews()
    }
    
    override func styleViews() {
        super.styleViews()
    }

    override func setup(_ viewModel: TransactionCellViewModel) {
        super.setup(viewModel)
    }
}
